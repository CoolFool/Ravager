#from ravager import *

#PORT = int(os.environ.get('PORT', '8443'))


"""
def main():
    updater.start_webhook(listen="127.0.0.1",
                          port=PORT,
                          url_path=TOKEN)
    LOGGER.info("Ravager Bot Up and Running!")
    updater.bot.set_webhook(APP_URL + TOKEN)
    updater.idle()


main()
"""